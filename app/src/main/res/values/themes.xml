<resources>
    <!-- Base application theme. -->
    <style name="Theme.TimeToCook" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pink_dark</item>
        <item name="colorPrimaryVariant">@color/pink_light</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red_pastel_dark</item>
        <item name="colorSecondaryVariant">@color/red_pastel_light</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Color surface -->
        <item name="colorSurface">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/amatic_sc_bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowBackground">@color/pink_dark</item>
        <item name="android:includeFontPadding">false</item>
        <!-- Animation -->
        <item name="android:windowActivityTransitions">true</item>
    </style>
</resources>